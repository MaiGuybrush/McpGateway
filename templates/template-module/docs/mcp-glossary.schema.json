{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "McpGatewayGlossary",
  "description": "Schema for McpGateway project-level custom domain glossary (mcp-glossary.json)",
  "type": "object",
  "properties": {
    "$schema": {
      "type": "string"
    },
    "version": {
      "type": "string",
      "default": "1.0.0"
    },
    "terms": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name", "pascalName", "description"],
        "properties": {
          "name": {
            "type": "string",
            "description": "Standard camelCase property name (e.g., carrierSlotNo)"
          },
          "pascalName": {
            "type": "string",
            "description": "Standard PascalCase C# property name (e.g., CarrierSlotNo)"
          },
          "description": {
            "type": "string",
            "description": "Clear explanation of the property semantics for LLM consumption"
          },
          "category": {
            "type": "string",
            "description": "Domain category (e.g., Entity, Metric, Status, Quality)"
          },
          "aliases": {
            "type": "array",
            "items": {
              "type": "string"
            },
            "description": "List of legacy/synonym property names that should trigger diagnostic warnings"
          }
        }
      }
    }
  },
  "required": ["terms"]
}
